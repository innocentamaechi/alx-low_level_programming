#include <stdio.h>

/** main - This program write all thr single digits from 0 - 
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int num;

	for(num = 0; num < 10; num++)
	{
		printf("%d", num);
	}

	printf("\n");

	return (0);
}
